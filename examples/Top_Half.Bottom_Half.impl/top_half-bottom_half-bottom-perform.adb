with Ada.Text_IO; use Ada.Text_IO;

separate (Top_Half.Bottom_Half.Bottom)
procedure Perform
  (X : Integer) is
begin
   Put_Line ("Top_Half.Bottom_Half.Bottom.Perform called with " & X'Img);
end Perform;
